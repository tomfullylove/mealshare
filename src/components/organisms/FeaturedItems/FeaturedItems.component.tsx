import React from 'react';
import {ScrollContainer, Container} from './assets/styles';

const FeaturedItems: React.FC = () => {
  return <ScrollContainer horizontal contentInset={{right: 16, left: 16}} />;
};

export default FeaturedItems;
